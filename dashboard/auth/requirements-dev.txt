# Development dependencies - NOT included in production Docker image
# Install with: pip install -r requirements-dev.txt

# Runtime deps (auth has no requirements.txt)
flask
requests

# Testing
pytest>=8.0
pytest-cov>=4.0
