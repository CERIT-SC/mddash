[project]
name = "dashboard-auth"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "flask",
    "requests",
    "gunicorn",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "watchdog",
    "ty",
]
