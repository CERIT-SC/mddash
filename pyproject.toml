[tool.uv.workspace]
members = ["dashboard/api", "dashboard/auth", "mdrun-api", "tuner"]

# This workspace venv is for IDE type resolution only.
# Per-component venvs remain the source of truth for runtime behavior.

[dependency-groups]
dev = [
    # pre_spawn_hook.py type support
    "jupyterhub",
    "jupyterhub-kubespawner",
    "kubernetes-asyncio",
]
