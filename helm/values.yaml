jupyterhub:
  hub:
    config:
      JupyterHub:
        admin_access: true
        authenticator_class: dummy
      DummyAuthenticator:
        password: "admin"
    
  singleuser:
    image:
      name: jupyter/base-notebook
      tag: "latest"
    defaultUrl: "/lab"
    
  proxy:
    secretToken: "your-secret-token-here-change-me"
    
  ingress:
    enabled: false
