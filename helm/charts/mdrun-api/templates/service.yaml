apiVersion: v1
kind: Service
metadata:
  name: mdrun-api
  labels:
    app: mdrun-api
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
  selector:
    app: mdrun-api
