# Replaces OIDC auth with DummyAuthenticator and disables ingress.
jupyterhub:
  hub:
    config:
      JupyterHub:
        authenticator_class: dummy
    extraConfig:
      # Override the OIDC client setup — not needed with DummyAuthenticator
      oauth-client-secret: ""
  ingress:
    enabled: false
